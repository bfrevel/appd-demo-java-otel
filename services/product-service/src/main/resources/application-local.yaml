spring:

  jpa:
    hibernate:
      ddl-auto: update
    platform: postgres
    show-sql: true

  datasource:
    url: jdbc:postgresql://localhost:5422/postgres
    username: postgres
    password: postgrespw

server:
  port : 8020